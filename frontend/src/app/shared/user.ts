export interface User {
    username: string
    password: string
    email: string
    avatarURL?: string
    name: string
    code: string
    // icon: string
    // channels: string[]
    // friends: string[]
}
